# Django Settings
SECRET_KEY=ft%acp0sik%iggzvj7r)gd4b&+0xao+j9bt3gees*a9b#=(l!9
DEBUG=False
ALLOWED_HOSTS=mandadmedical.com,www.mandadmedical.com

# Database (PostgreSQL)
# Option 1: Use DATABASE_URL (recommended for cloud deployments)
# DATABASE_URL=postgresql://user:password@localhost:5432/mandad

# Option 2: Individual database settings
DB_ENGINE=django.db.backends.postgresql
DB_NAME=mandad
DB_USER=mandad_prod
DB_PASSWORD=verySecure
DB_HOST=localhost
DB_PORT=5432

# Email Configuration (SMTP)
EMAIL_HOST=smtp.comcast.net
EMAIL_PORT=587
EMAIL_HOST_USER=mandadmed@comcast.net
EMAIL_HOST_PASSWORD=your_email_password
ADMIN_EMAIL=mandadmed@comcast.net

# Optional: For production, set a custom admin email
# ADMIN_EMAIL=admin@mandadmedical.com
