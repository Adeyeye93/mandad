# Django Settings
SECRET_KEY=change-me-generate-a-new-secret-key
DEBUG=False
ALLOWED_HOSTS=mandadmedical.com,www.mandadmedical.com

# Database (PostgreSQL)
# Option 1: Use DATABASE_URL (recommended for cloud deployments)
# DATABASE_URL=postgresql://user:password@localhost:5432/mandad

# Option 2: Individual database settings
DB_ENGINE=django.db.backends.postgresql
DB_NAME=mandad
DB_USER=mandad_prod
DB_PASSWORD=change-me
DB_HOST=localhost
DB_PORT=5432

# Email Configuration (SMTP — Zoho Mail)
EMAIL_HOST=smtppro.zoho.com
EMAIL_PORT=465
EMAIL_HOST_USER=sales@mandadmedical.com
EMAIL_HOST_PASSWORD=change-me
ADMIN_EMAIL=sales@mandadmedical.com
